<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:fab="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/chef_event_dishes_fragment_container"
    android:background="@color/eat_white">
    <android.support.v4.widget.SwipeRefreshLayout

        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/chef_events_dishes_swipe_to_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent">


    <ListView
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:id="@+id/chef_events_dishes_list_view"
        android:layout_gravity="center_horizontal"
        android:layout_weight="8" />
    </android.support.v4.widget.SwipeRefreshLayout>


    <com.melnykov.fab.FloatingActionButton
        android:id="@+id/chef_events_dishes_floating_add_button"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="left|bottom"
        android:layout_marginBottom="20dp"

        android:text="+"
        android:textColor="@drawable/plus_icon"
        android:layout_marginLeft="20dp"
        android:background="@drawable/yellow_circle"
        android:src="@drawable/fab_plus"
        />
    <ProgressBar
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:id="@+id/chef_events_dishes_progress_bar"
        android:layout_gravity="center_horizontal|top" />

</FrameLayout>